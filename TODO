Initial version:
    ☐ Provide a simple way to add new entries to the word list
    ☐ Allow the word list to live in a default location
    ☐ Allow for word lists to be loaded from multiple spots
    ☐ Determine if the `cspell` approach can be integrated into ts source or if it must be run from shell
    ☐ Provide a simple interface to edit/modify the current replacement list
    ☐ Build the project into a proper node CLI
